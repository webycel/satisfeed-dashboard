== README

How to run the project on local (assuming rails is already installed)

* Clone/Fork the repository
* Run <tt>bundle install</tt>
* Run <tt>rake db:migrate</tt>
* Run <tt>rails s</tt> to start the local server
